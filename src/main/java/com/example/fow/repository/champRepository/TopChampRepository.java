package com.example.fow.repository.champRepository;

import com.example.fow.entity.champEntity.TopChamp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TopChampRepository extends JpaRepository<TopChamp,Integer> {
}
