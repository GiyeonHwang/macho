package com.example.fow.repository.champRepository;

import com.example.fow.entity.champEntity.SupportChamp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportChampRepository extends JpaRepository<SupportChamp,Integer> {
}
